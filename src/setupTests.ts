
// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';

// A diretiva de referência types="@testing-library/jest-dom" em vite-env.d.ts
// e a importação acima devem ser suficientes para carregar os tipos.
// Se os erros persistirem, pode ser necessário revisar a configuração do tsconfig.json
// para garantir que "jest" and "@testing-library/jest-dom" estejam em compilerOptions.types
// e que setupTests.ts esteja corretamente configurado no seu ambiente de teste (ex: jest.config.js ou vite.config.js).

